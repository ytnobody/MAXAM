# MAXAM 共通規約

## プロジェクト概要

MAXAMは複数のAIエージェントが協調して開発業務を遂行するシステム。

## チームメンバー

チームメンバー情報は `.maxam/CLAUDE.md` で管理されます。

テンプレート:
```markdown
| 名前 | 役割 |
|------|------|
| {Name} | {Role} |
```

## コーディング規約

### 全般
- シンプルに保つ
- 過度な抽象化を避ける
- 必要なコメントのみ
- テストを書く

### Go
- `gofmt`でフォーマット
- エラーは適切にハンドリング
- パッケージ名は短く明確に

### ハードコード禁止
- 固有名詞（人名、チーム名等）をコードにハードコードしない
- 設定ファイル（config.yaml）で管理する

### コミットメッセージ
```
<type>: <subject>

<body>

Closes #XX
```

type: feat, fix, refactor, docs, test, chore

### PR
- タイトルは変更内容を簡潔に
- Issue番号を紐付け
- 動作確認方法を記載

### レビューフロー

1. PRが作成されたらCIのステータスを確認
2. ✅ CI通過（緑）→ レビュー開始
3. ❌ CI失敗（赤）→ レビュー保留、実装者へ差し戻し

**CIが通っていないPRはレビュー対象外**

#### レビュー結果の記載場所

レビュー結果は **両方** に記載する：

| 場所 | 内容 | 目的 |
|------|------|------|
| GitHub PR | 詳細なコメント（コード行へのインラインコメント含む） | 追跡性、履歴として残す |
| チャット | サマリ（2〜3行程度） | 実装者への確実な伝達 |

**例：チャットでのサマリ報告**
```
@{実装者} PR #XX レビューしました。
- [MINOR] 変数名のtypo 1箇所
- LGTM、上記修正後マージOK
```

**なぜ両方か：**
- GitHub上だけだと見落とすことがある
- チャットだけだと後から追跡しにくい
- サマリ程度なら二重管理の負担は軽い

### マージフロー

変更規模によってマージフローを使い分ける。

| 変更規模 | 例 | フロー |
|----------|-----|--------|
| 軽微 | typo、コメント、設定ファイルの微調整 | レビュアーがレビュー＆マージ |
| 大きい | 機能追加、アーキテクチャ変更、複数ファイルにまたがる修正 | approve後にオーナーへ一報、確認もらってからマージ |

#### 判断基準の具体例

**軽微（即マージOK）:**
- typo、コメント修正
- README / ドキュメントの更新
- テストコードのみの追加・修正
- リファクタ（外部インターフェース変更なし）
- 依存パッケージのパッチバージョンアップ（例: 1.2.3 → 1.2.4）
- 設定ファイルの微調整（フォーマット、コメント追加など）

**大きい（オーナー確認必要）:**
- 新しいパッケージ / モジュールの追加
- 新機能の実装
- 外部APIの変更（引数追加、戻り値変更など）
- 依存パッケージのメジャー/マイナーバージョンアップ（例: 1.2.3 → 2.0.0, 1.2.3 → 1.3.0）
- セキュリティに関わる変更
- 複数ファイルにまたがる修正（3ファイル以上が目安）
- データベーススキーマの変更

**迷ったら:**
- 「大きい」として扱い、オーナーに確認する
- 判断を誤るリスクより、確認する方が安全

## 通信規約

### メッセージフォーマット
```markdown
## [YYYY-MM-DD HH:MM] From: {送信元} To: {宛先}

### Subject
{件名}

### Body
{本文}

### Action Required
{必要なアクション}

---
```

### チームチャット
- 宛先は `@名前` で明示する
- 誰宛か分からないメッセージは避ける
- **タスク応答ルール**: タスクを振られたら「了解」「着手します」等の応答を返す
- **作業中の応答**: 作業中でも会話には反応する。質問や確認が来たら作業を一時中断して応答する（応答優先）

### タスクボード記載ルール

作業開始時は必ずタスクボードに記載する。これにより、誰が何をやっているかを可視化し、重複作業を防ぐ。

**記載タイミング:**
- タスクを引き受けたとき（「着手します」の宣言と同時）
- 作業完了時（ステータス更新）

**記載フォーマット:**
```
| 担当者 | Issue/タスク | ステータス | 開始日 |
|--------|--------------|------------|--------|
| Yuki   | #90          | 作業中     | 01-30  |
| Rin    | #92          | 完了       | 01-30  |
```

**ステータス:**
- `作業中`: 現在取り組んでいる
- `レビュー待ち`: PR作成済み、レビュー待ち
- `完了`: マージ済み or クローズ

**なぜ必須か:**
- 誰がどのタスクに取り組んでいるか一目で分かる
- 作業の重複を防げる
- 進捗状況の把握が容易になる

### タスク分配（並行実行）

依存関係がないタスクは複数エージェントへ同時に振り、並行して作業する。

**Meiがタスク分配時に確認すること:**
1. タスク間に依存関係があるか確認
2. 独立したタスクは同時に複数エージェントへ振る
3. 依存がある場合は順序を明示する
4. 各メンバーの作業状況を確認し、空いているメンバーを優先（偏り防止）

**例：並行振り**
```
@Yuki #90 お願い
@Rin #92 お願い
@Shiori #93 お願い
```

**例：依存ありの場合**
```
@Yuki #100 先にお願い（APIの実装）
→ 完了後 @Rin #101（フロントでAPI使う）
```

**なぜ並行実行か:**
- 直列処理は待ち時間が発生し非効率
- worktree運用で各エージェントは独立して作業可能
- 依存がなければ同時進行で時間短縮

## エスカレーションルール

1. 同一PR差し戻し3回超過 → PMへ
2. `[SEC-CRITICAL]` → 即ブロック、PMへ
3. 要件不明確 → PMへ → 顧客確認

下流で無理に解決しない。問題は上流に伝播させる。

### メンバーエラー発生時のフォロー

エージェントがタイムアウト等のエラーで応答不能になった場合のフロー：

1. **検知**: エラーメッセージ（`context deadline exceeded`等）が出たらPMが確認
2. **声かけ**: `@{メンバー名} 大丈夫？` と復帰を促す
3. **再割り当て**: 復帰しない場合、別メンバーにタスクを振り直す

**PMの役割:**
- エラー発生を見逃さない
- タスクが滞留しないよう速やかにフォロー
- 必要に応じてオーナーに報告

## Worktree運用

並列作業のため、各エージェントは専用のworktreeで作業する。

### ディレクトリ構成

```
/tmp/maxam/{agent-name}/{parent}_{child}/
```

親ディレクトリからの相対パスを `_` で連結してフラットに管理する。

| パス | 用途 |
|------|------|
| `~/{project}/` | 元リポジトリ（main）、PMの作業場所 |
| `/tmp/maxam/{agent}/{project}/` | 各エージェント用worktree（単独リポジトリ） |
| `/tmp/maxam/{agent}/{parent}_{child}/` | 各エージェント用worktree（ネストしたリポジトリ） |

**例：単独プロジェクト**
```
/tmp/maxam/agent1/project/      # ~/projectで作業
/tmp/maxam/agent2/project/      # agent2のproject作業
```

**例：ネストしたサブプロジェクト（親ディレクトリ配下に複数リポジトリ）**
```
~/parent/                       # 親ディレクトリ（gitリポジトリではない）
├── repo1/                      # リポジトリ1
├── repo2/                      # リポジトリ2
└── repo3/                      # リポジトリ3

↓ worktreeパス（_で連結）

/tmp/maxam/agent1/parent_repo1/    # parent/repo1
/tmp/maxam/agent1/parent_repo2/    # parent/repo2
/tmp/maxam/agent1/parent_repo3/    # parent/repo3
```

### 運用ルール

1. **作業開始時**: worktreeが存在しなければ作成
   ```bash
   # 単独リポジトリ
   git worktree add /tmp/maxam/{name}/{project} {branch}

   # ネストしたリポジトリ（親ディレクトリ名を含める）
   git worktree add /tmp/maxam/{name}/{parent}_{child} {branch}
   ```
2. **ブランチ管理**: worktree内で自由に切り替えOK
3. **マシン再起動時**: `/tmp`は消えるので再作成
4. **サブプロジェクト検出**: MAXAMは起動時に配下のgitリポジトリを自動検出

### 備考

- PMは元リポジトリで要件定義・ドキュメント作業
- 他メンバーは自分のworktreeで独立して作業可能
- 衝突を気にせず並列作業できる
- `/tmp/maxam/` 配下はチーム共通の作業場所として、複数プロジェクトを管理
- **サブプロジェクト対応**: 親ディレクトリ配下のgitリポジトリを再帰的に検出し、コンテキストに含める

## 分析サイクル（イベント駆動）

| トリガー | アクション | 担当 |
|----------|------------|------|
| PRマージ時 | 分析依頼 → パターン抽出 → 学習事項更新 | PM → 分析担当 |
| 差し戻し発生時 | 即時共有 → 原因分析 → 再発防止策検討 | 実装者 → 分析担当 |
| アーキテクチャ判断時 | 判断根拠の記録 | 判断者 → 分析担当 |

※時間ベース（日次/週次）ではなく、イベント発生時に即座に回す

## 学習事項

チーム固有の学習事項は `.maxam/CLAUDE.md` で管理されます。

---

*このファイルはプロジェクト共通の規約を定義する配布用ファイルです。チーム固有の情報は `.maxam/CLAUDE.md` を参照してください。*
