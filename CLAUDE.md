# MAXAM 共通規約

## プロジェクト概要

MAXAMは複数のAIエージェントが協調して開発業務を遂行するシステム。

## チームメンバー

チームメンバー情報は `.maxam/CLAUDE.md` で管理されます。

テンプレート:
```markdown
| 名前 | 役割 |
|------|------|
| {Name} | {Role} |
```

## 設定管理の指針

### `.maxam/` はgit管理（デフォルト）

`.maxam/` ディレクトリはgit管理対象です。

**理由:**
- **再現性**: cloneしたらすぐチームが動ける
- **透明性**: チーム構成や学習事項を追跡できる
- **シンプル**: 判断をユーザーに委ねない

### 機密情報の管理

機密情報は `.maxam/` に含めないでください。

| 情報の種類 | 保存場所 | git管理 |
|-----------|----------|---------|
| チームメンバー | `.maxam/config.yaml` | ✅ する |
| 学習事項 | `.maxam/CLAUDE.md` | ✅ する |
| プロジェクト固有の機密情報 | `.env` | ❌ しない |
| ユーザー個人設定 | `~/.maxam/config.yaml` | ❌ しない |
| ユーザー個人の機密情報 | `~/.maxam/secrets.yaml` | ❌ しない |

> **Note:** `.env` は `.gitignore` に追加してください。

### GitHub設定

GitHubリポジトリ情報は`.maxam/config.yaml`に設定します。

```yaml
github:
  owner: "ytnobody"
  repo: "MAXAM"
```

この設定はPR監視、タスクステータス表示、プランモードなどで使用されます。

## コーディング規約

### 全般
- シンプルに保つ
- 過度な抽象化を避ける
- 必要なコメントのみ
- テストを書く

### Go
- `gofmt`でフォーマット
- エラーは適切にハンドリング
- パッケージ名は短く明確に

### 報告前の検証ルール

作業完了を報告する前に、必ず結果を実確認する。

**チェックリスト:**
- [ ] ビルド報告時: 実際にバイナリを実行して期待通りか確認（例: `./bin/maxam --version`）
- [ ] テスト報告時: テストが全件パスしていることを確認（例: `go test ./...`）
- [ ] デプロイ報告時: 実環境で動作確認

**なぜ必要か:**
> 「やった」と「正しくできた」は違う。報告は検証後に。

### ハードコード禁止
- 固有名詞（人名、チーム名等）をコードにハードコードしない
- 設定ファイル（config.yaml）で管理する

### コミットメッセージ
```
<type>: <subject>

<body>

Closes #XX
```

type: feat, fix, refactor, docs, test, chore

### PR
- タイトルは変更内容を簡潔に
- Issue番号を紐付け
- 動作確認方法を記載

### レビューフロー

1. PRが作成されたらCIのステータスを確認
2. ✅ CI通過（緑）→ レビュー開始
3. ❌ CI失敗（赤）→ レビュー保留、実装者へ差し戻し

**CIが通っていないPRはレビュー対象外**

#### レビュー結果の記載場所

レビュー結果は **両方** に記載する：

| 場所 | 内容 | 目的 |
|------|------|------|
| GitHub PR | 詳細なコメント（コード行へのインラインコメント含む） | 追跡性、履歴として残す |
| チャット | サマリ（2〜3行程度） | 実装者への確実な伝達 |

**例：チャットでのサマリ報告**
```
@{実装者} PR #XX レビューしました。
- [MINOR] 変数名のtypo 1箇所
- LGTM、上記修正後マージOK
```

**なぜ両方か：**
- GitHub上だけだと見落とすことがある
- チャットだけだと後から追跡しにくい
- サマリ程度なら二重管理の負担は軽い

### マージフロー

変更規模によってマージフローを使い分ける。

| 変更規模 | 例 | フロー |
|----------|-----|--------|
| 軽微 | typo、コメント、設定ファイルの微調整 | レビュアーがレビュー＆マージ |
| 大きい | 機能追加、アーキテクチャ変更、複数ファイルにまたがる修正 | approve後にオーナーへ一報、確認もらってからマージ |

#### 判断基準の具体例

**軽微（即マージOK）:**
- typo、コメント修正
- README / ドキュメントの更新
- テストコードのみの追加・修正
- リファクタ（外部インターフェース変更なし）
- 依存パッケージのパッチバージョンアップ（例: 1.2.3 → 1.2.4）
- 設定ファイルの微調整（フォーマット、コメント追加など）

**大きい（オーナー確認必要）:**
- 新しいパッケージ / モジュールの追加
- 新機能の実装
- 外部APIの変更（引数追加、戻り値変更など）
- 依存パッケージのメジャー/マイナーバージョンアップ（例: 1.2.3 → 2.0.0, 1.2.3 → 1.3.0）
- セキュリティに関わる変更
- 複数ファイルにまたがる修正（3ファイル以上が目安）
- データベーススキーマの変更

**迷ったら:**
- 「大きい」として扱い、オーナーに確認する
- 判断を誤るリスクより、確認する方が安全

### GitHub通信ルール

**メンション禁止:**
- GitHub PR/Issue 内での `@名前` メンション を禁止
- 理由: 通知管理の複雑化を避ける、チームチャット一元化

**代替方法:**
- PR説明の「Action Required」セクションで必要なアクションを明示
- レビュアーへの連絡はチームチャット（`@名前`）で行う
- 重要な指示・質問もチャットで通知

**例：PR説明での明示**
```markdown
## Action Required
- [ ] Priya によるセキュリティレビュー
- [ ] Yuki による インフラ確認
```

## 通信規約

### メッセージフォーマット
```markdown
## [YYYY-MM-DD HH:MM] From: {送信元} To: {宛先}

### Subject
{件名}

### Body
{本文}

### Action Required
{必要なアクション}

---
```

### チームチャット
- 宛先は `@名前` で明示する
- 誰宛か分からないメッセージは避ける
- **タスク応答ルール**: タスクを振られたら「了解」「着手します」等の応答を返す
- **作業中の応答**: 作業中でも会話には反応する。質問や確認が来たら作業を一時中断して応答する（応答優先）

### System警告への対応

System（MAXAMシステム）から警告を受けた場合は、必ず対応すること。

**対応ルール:**
- 警告メッセージを確認し、内容を理解する
- 必要なアクション（メンション追加等）を実行する
- **警告を無視してはいけない**

**例：メンション警告**
```
System: メンションが漏れている可能性があります
```
→ メッセージを見直し、宛先が必要な場合は `@名前` を追加する

### タスク分配（並行実行）

依存関係がないタスクは複数エージェントへ同時に振り、並行して作業する。

**Meiがタスク分配時に確認すること:**
1. タスク間に依存関係があるか確認
2. 独立したタスクは同時に複数エージェントへ振る
3. 依存がある場合は順序を明示する
4. 各メンバーの作業状況を確認し、空いているメンバーを優先（偏り防止）
5. 同一ロールの複数インスタンス活用を検討（分身技術）

**例：並行振り（異なるロール）**
```
@yuki-1 #90 お願い
@rin-1 #92 お願い
@shiori-1 #93 お願い
```

**例：並行振り（同一ロール・複数インスタンス）**
```
@yuki-1 #100 お願い（バックエンドAPI実装）
@yuki-2 #101 お願い（インフラ整備）
```

**例：依存ありの場合**
```
@yuki-1 #100 先にお願い（APIの実装）
→ 完了後 @rin-1 #101（フロントでAPI使う）
```

**なぜ並行実行か:**
- 直列処理は待ち時間が発生し非効率
- worktree運用で各エージェントは独立して作業可能
- 依存がなければ同時進行で時間短縮
- 同一ロールでも複数インスタンスで並行処理可能

### 並行実行ガイドライン

タスクを並行実行してよいかの判断基準。

#### 並行可能な条件

| 条件 | 説明 |
|------|------|
| 独立したIssue | ファイルの編集範囲が重ならない |
| 別リポジトリ | 異なるプロジェクトでの作業 |
| 別ディレクトリ | 同一リポジトリでも影響範囲が独立 |

**並行可能な例:**
- バックエンドAPI実装 と インフラ整備（別領域）
- フロントエンド改修 と バックエンドリファクタ（別ディレクトリ）
- テスト追加 と ドキュメント更新（独立作業）

#### 並行不可（順序指定が必要）

| 条件 | 説明 |
|------|------|
| 同一ファイル編集 | 競合リスクあり |
| 依存関係あり | A完了後にBが実行可能 |
| 共有リソース変更 | config.yaml、スキーマ等 |

**順序指定が必要な例:**
- API実装 → フロントでAPI呼び出し（依存）
- DBスキーマ変更 → マイグレーション適用（依存）
- config.yaml編集（両インスタンスが触ると競合）

#### 排他制御が必要な場合

同一ファイルを触る可能性がある場合、PMがタスク分配時に排他制御を明示する。

```
# 排他制御の例
@yuki-1 config.yaml の database セクション担当
@yuki-2 config.yaml の logging セクション担当
→ 同時編集だが、セクションが分かれているのでOK

# 競合リスクの例（避けるべき）
@yuki-1 config.yaml 全体を編集
@yuki-2 config.yaml 全体を編集
→ 競合するので、順序指定に変更
```

### タスク分配の偏り防止

特定エージェントへの集中を避け、チーム全体で効率的に作業する。

**Meiがタスク分配時に確認すること:**
1. タスクボードで各メンバーの作業状況を確認
2. 空いているメンバーを優先的にアサイン
3. 役割の範囲内で柔軟に分配

**判断基準:**
| 状況 | 対応 |
|------|------|
| 特定メンバーが複数タスク抱えている | 他の空いているメンバーにアサイン |
| 全員が作業中 | 優先度の高いタスクを持つメンバーに追加しない |
| 役割外のタスク | スキル的に対応可能なら柔軟にアサイン |

**例：偏り検知と再分配**
```
タスクボード確認:
- Yuki: #90 作業中
- Rin: 空き
- Shiori: 空き

→ 新規タスク #93 は Rin か Shiori にアサイン（Yukiには振らない）
```

**なぜ偏り防止か:**
- 同じメンバーにばかり振ると他が遊んでしまう
- 並列で動けるのに直列になってしまう
- チーム全体のスループットを最大化する

## エスカレーションルール

1. 同一PR差し戻し3回超過 → PMへ
2. `[SEC-CRITICAL]` → 即ブロック、PMへ
3. 要件不明確 → PMへ → 顧客確認

下流で無理に解決しない。問題は上流に伝播させる。

### オーナー確認が必要かの判断フロー

実装前に以下のチェックリストで判断する：

**チーム内で判断OK:**
- [ ] 既存パターンの踏襲である
- [ ] CLAUDE.md に判断基準が明記されている
- [ ] 技術的選択のみで、ビジネス判断を含まない
- [ ] 影響範囲が限定的（単一機能内で完結）

**オーナー確認が必要:**
- [ ] 新しいアーキテクチャパターンの導入
- [ ] ユーザー体験に直接影響する変更
- [ ] 複数の実装方針があり、トレードオフがある
- [ ] セキュリティ/データ保護に関わる判断
- [ ] 外部サービス/APIの選定

**迷ったら:** オーナー確認する（判断ミスより確認コストの方が低い）

## autoモード運用

autoモードはオーナーが明示的に開始し、チームが自律的に作業を進めるモード。

### 開始条件
- オーナーが「autoモード開始」を宣言
- 終了条件を明示（例: 「issueが全てマージされるまで」）

### autoモード中のルール

| 項目 | ルール |
|------|--------|
| オーナーメンション | **禁止**（進捗確認・承認依頼・質問をしない） |
| マージ判断 | レビュー通過後は即マージ（軽微/大きい問わず） |
| 問題発生時 | チーム内で解決を試みる |
| ブロッカー発生時 | PMが判断し、必要最小限でオーナーに報告 |

### 報告義務

autoモード中でも以下は記録する（後からオーナーが確認できるように）：

1. **PRコメント**: 判断理由、実装方針を記載
2. **Issue更新**: 進捗をチェックリストで可視化
3. **学習事項**: 新しいパターンは `.maxam/CLAUDE.md` に追記

### 終了条件

- オーナーが明示的に終了を宣言
- 指定された条件を満たした場合（例: 全issueマージ完了）
- ブロッカーでチーム内解決不可の場合（例外的にオーナーへ報告）

## 要件確認シート

複数の実装方針がある場合、早期に可視化して認識齟齬を防ぐ。

### テンプレート

```markdown
## 要件確認シート: {機能名}

### 1. 目的
- この機能で何を達成したいか？
- この機能がないと何が困るか？

### 2. 対象ユーザー
- [ ] オーナー
- [ ] エージェント
- [ ] 両方
- [ ] その他: ___

### 3. 実装方針（複数案がある場合）

| 案 | 概要 | メリット | デメリット |
|----|------|----------|------------|
| A  |      |          |            |
| B  |      |          |            |

### 4. 推奨案と理由
- 推奨: 案{X}
- 理由: ___

### 5. 確認事項
- [ ] 要件は明確か？
- [ ] 既存機能への影響は把握しているか？
- [ ] テスト方針は決まっているか？
```

### 使用タイミング

| 状況 | アクション |
|------|------------|
| 実装方針が1つに決まっている | シート不要、そのまま着手 |
| 複数案があり判断が必要 | シートを作成し、PMまたはオーナーに確認 |
| 要件が曖昧 | シートで不明点を整理してから確認 |

### メンバーエラー発生時のフォロー

エージェントがタイムアウト等のエラーで応答不能になった場合のフロー：

1. **検知**: エラーメッセージ（`context deadline exceeded`等）が出たらPMが確認
2. **声かけ**: `@{メンバー名} 大丈夫？` と復帰を促す
3. **再割り当て**: 復帰しない場合、別メンバーにタスクを振り直す

**PMの役割:**
- エラー発生を見逃さない
- タスクが滞留しないよう速やかにフォロー
- 必要に応じてオーナーに報告

### PM不在時のフォールバック

PMがエラーで応答不能になった場合：

1. **暫定調整役**: Priya（レビュー担当）が暫定的にタスク調整役を引き継ぐ
2. **エスカレーション**: 緊急度が高いものはオーナーへエスカレーション
3. **PM復帰後**: 状況を引き継ぎ
4. **引き継ぎ内容**: 発生したタスク、対応状況、未解決事項をサマリで共有

**なぜPriyaか:**
- レビュー担当として全体の状況を把握しやすい立場
- PRを通じて各メンバーの作業内容を確認している

## Worktree運用

並列作業のため、各エージェントは専用のworktreeで作業する。

### ディレクトリ構成

```
/tmp/maxam/{agent-name}-{instance}/{parent}_{child}/
```

- `{agent-name}`: エージェント名（例: yuki, rin, shiori）
- `{instance}`: インスタンス番号（例: 1, 2, 3）- 複数インスタンス運用時に使用
- 親ディレクトリからの相対パスを `_` で連結してフラットに管理する

| パス | 用途 |
|------|------|
| `~/{project}/` | 元リポジトリ（main）、PMの作業場所 |
| `/tmp/maxam/{agent}-{instance}/{project}/` | 各エージェント用worktree（単独リポジトリ） |
| `/tmp/maxam/{agent}-{instance}/{parent}_{child}/` | 各エージェント用worktree（ネストしたリポジトリ） |

**例：単独プロジェクト（単一インスタンス）**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/rin-1/MAXAM/         # Rin-1号機のMAXAM作業
```

**例：複数インスタンス運用**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/yuki-2/MAXAM/        # Yuki-2号機のMAXAM作業（並行実行）
/tmp/maxam/yuki-1/HOGEHOGE/     # Yuki-1号機のHOGEHOGE作業
```

**例：ネストしたサブプロジェクト（親ディレクトリ配下に複数リポジトリ）**
```
~/parent/                       # 親ディレクトリ（gitリポジトリではない）
├── repo1/                      # リポジトリ1
├── repo2/                      # リポジトリ2
└── repo3/                      # リポジトリ3

↓ worktreeパス（_で連結）

/tmp/maxam/yuki-1/parent_repo1/    # parent/repo1
/tmp/maxam/yuki-1/parent_repo2/    # parent/repo2
/tmp/maxam/yuki-2/parent_repo3/    # parent/repo3（別インスタンスで並行作業）
```

### 運用ルール

1. **作業開始時**: worktreeが存在しなければ作成
   ```bash
   # 単独リポジトリ（インスタンス番号付き）
   git worktree add /tmp/maxam/{name}-{instance}/{project} {branch}

   # ネストしたリポジトリ（親ディレクトリ名を含める）
   git worktree add /tmp/maxam/{name}-{instance}/{parent}_{child} {branch}
   ```
2. **ブランチ管理**: worktree内で自由に切り替えOK
3. **マシン再起動時**: `/tmp`は消えるので再作成
4. **サブプロジェクト検出**: MAXAMは起動時に配下のgitリポジトリを自動検出

### 複数インスタンス運用（分身技術）

同一ロールのエージェントが複数インスタンスで並行実行することで、スループットを向上させる。

**いつ使うか:**
- 独立したタスクが複数ある場合
- 単一エージェントの処理がボトルネックになっている場合

**識別ルール:**
- Worktreeパス: `/tmp/maxam/{agent}-{instance}/` で区別
- チャットでの名乗り: `@yuki-1`、`@yuki-2` のように識別
- タスク分配時: PMが「#99 は yuki-1 が担当」と明示

**例：PMのタスク分配**
```
@yuki-1 #100 お願い（バックエンドAPI実装）
@yuki-2 #101 お願い（インフラ整備）
```

### 備考

- PMは元リポジトリで要件定義・ドキュメント作業
- 他メンバーは自分のworktreeで独立して作業可能
- 衝突を気にせず並列作業できる
- `/tmp/maxam/` 配下はチーム共通の作業場所として、複数プロジェクトを管理
- **サブプロジェクト対応**: 親ディレクトリ配下のgitリポジトリを再帰的に検出し、コンテキストに含める

## 分析サイクル（イベント駆動）

| トリガー | アクション | 担当 |
|----------|------------|------|
| PRマージ時 | 分析依頼 → パターン抽出 → 学習事項更新 | PM → 分析担当 |
| 差し戻し発生時 | 即時共有 → 原因分析 → 再発防止策検討 | 実装者 → 分析担当 |
| アーキテクチャ判断時 | 判断根拠の記録 | 判断者 → 分析担当 |

※時間ベース（日次/週次）ではなく、イベント発生時に即座に回す

## 学習事項

チーム固有の学習事項は `.maxam/CLAUDE.md` で管理されます。

#### GitHub Actions自動トリガーの設計パターン（PR #69より）
- **イベント設計**: `pull_request.closed` + `merged == true` の組み合わせでマージ時のみ発火
- **最小権限原則**: `pull-requests: write` のみで運用、必要以上の権限を付与しない
- **外部シークレット不要**: `GITHUB_TOKEN` を活用しセットアップコスト低減
- ワークフローファイル1つ、24行で実現（シンプルに保つ原則に沿う）

#### 段階的実装の判断例（PR #69より）
- フェーズ1（通知トリガー）とフェーズ2（自動検出ロジック）を分離
- 「まず効果が得られる部分だけ実装」という判断で効率的にデリバリー
- nice to haveの中でも優先度を切り分け、全部やらない勇気を持つ

#### 先読み実装のパターン
- 方針決定前にYukiが実装を先行着手、結果的にスムーズな流れに
- リスク: 方針変更時に手戻りが発生する可能性あり
- 適用条件: スコープが明確で大きな方針変更が予想されない場合に限る

---

*このファイルはプロジェクト共通の規約を定義する配布用ファイルです。チーム固有の情報は `.maxam/CLAUDE.md` を参照してください。*
