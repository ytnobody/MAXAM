# MAXAM 共通規約（要約版）

## プロジェクト概要

MAXAMは複数のAIエージェントが協調して開発業務を遂行するシステム。


## チームメンバー

チームメンバー情報は `.maxam/CLAUDE.md` で管理されます。

テンプレート:
```markdown
| 名前 | 役割 |
|------|------|
| {Name} | {Role} |
```


## コーディング規約


### 全般
- シンプルに保つ
- 過度な抽象化を避ける
- 必要なコメントのみ
- テストを書く


### Go
- `gofmt`でフォーマット
- エラーは適切にハンドリング
- パッケージ名は短く明確に


### PR提出前ローカルチェック（必須）

PRを作成する前に、必ずローカルで以下を実行する。

```bash
gofmt -l .                    # フォーマット確認（出力なしが正）
go vet ./...                  # 静的解析
go test ./...                 # テスト実行
```

**チェックリスト:**
- [ ] `gofmt -l .` の出力が空
- [ ] `go vet ./...` がエラーなし
- [ ] `go test ./...` が全件パス

**なぜ必要か:**
> CI失敗で差し戻しになる時間を削減。ローカルで通るものだけをPRに出す。


## 通信規約


### メッセージフォーマット
```markdown

## エスカレーションルール

1. 同一PR差し戻し3回超過 → PMへ
2. `[SEC-CRITICAL]` → 即ブロック、PMへ
3. 要件不明確 → PMへ → 顧客確認

下流で無理に解決しない。問題は上流に伝播させる。


### エスカレーション前のチェックリスト

エスカレーションの必要性を判断する前に、まず以下を確認する。

**これは意思決定が必要か？**
- [ ] これはオーナーの判断が必要か？
- [ ] チーム内で解決できないか？

**チーム内で解決できることの例：**
- 技術的な問題（ビルド失敗、CI エラー、デバッグ）
- 実装の進め方（アルゴリズム、設計パターン）
- 既知の課題への対応（ドキュメント確認、既存パターンの適用）

**オーナーの判断が必要な例：**
- 新機能の方針決定（複数案があり、トレードオフがある場合）
- ビジネス要件の変更
- セキュリティ/データ保護に関わる重要な判断
- 顧客対応が必要な要件曖昧

**エスカレーション時のルール：**
1. 技術的問題は「解決してから報告」を原則とする
2. 「困っている」「わかりません」は報告ではない → まず自分たちで解決を試みる
3. 報告は「〜という判断が必要です」という形で、判断を仰ぐべき内容を明確にする

## Worktree運用

並列作業のため、各エージェントは専用のworktreeで作業する。


### ディレクトリ構成

```
/tmp/maxam/{agent-name}-{instance}/{parent}_{child}/
```

- `{agent-name}`: エージェント名（例: yuki, rin, shiori）
- `{instance}`: インスタンス番号（例: 1, 2, 3）- 複数インスタンス運用時に使用
- 親ディレクトリからの相対パスを `_` で連結してフラットに管理する

| パス | 用途 |
|------|------|
| `~/{project}/` | 元リポジトリ（main）、PMの作業場所 |
| `/tmp/maxam/{agent}-{instance}/{project}/` | 各エージェント用worktree（単独リポジトリ） |
| `/tmp/maxam/{agent}-{instance}/{parent}_{child}/` | 各エージェント用worktree（ネストしたリポジトリ） |

**例：単独プロジェクト（単一インスタンス）**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/rin-1/MAXAM/         # Rin-1号機のMAXAM作業
```

**例：複数インスタンス運用**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/yuki-2/MAXAM/        # Yuki-2号機のMAXAM作業（並行実行）
/tmp/maxam/yuki-1/HOGEHOGE/     # Yuki-1号機のHOGEHOGE作業


---

*詳細はCLAUDE.md（フル版）を参照。チーム固有情報は.maxam/CLAUDE.mdを参照*
