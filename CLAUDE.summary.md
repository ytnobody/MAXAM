# MAXAM 共通規約（要約版）

## プロジェクト概要

MAXAMは複数のAIエージェントが協調して開発業務を遂行するシステム。


## チームメンバー

チームメンバー情報は `.maxam/CLAUDE.md` で管理されます。

テンプレート:
```markdown
| 名前 | 役割 |
|------|------|
| {Name} | {Role} |
```


## コーディング規約


### 全般
- シンプルに保つ
- 過度な抽象化を避ける
- 必要なコメントのみ
- テストを書く


### Go
- `gofmt`でフォーマット
- エラーは適切にハンドリング
- パッケージ名は短く明確に


### 報告前の検証ルール

作業完了を報告する前に、必ず結果を実確認する。

**チェックリスト:**
- [ ] ビルド報告時: 実際にバイナリを実行して期待通りか確認（例: `./bin/maxam --version`）
- [ ] テスト報告時: テストが全件パスしていることを確認（例: `go test ./...`）
- [ ] デプロイ報告時: 実環境で動作確認

**なぜ必要か:**
> 「やった」と「正しくできた」は違う。報告は検証後に。


### ハードコード禁止
- 固有名詞（人名、チーム名等）をコードにハードコードしない
- 設定ファイル（config.yaml）で管理する


### コミットメッセージ
```

## 通信規約


### メッセージフォーマット
```markdown

## エスカレーションルール

1. 同一PR差し戻し3回超過 → PMへ
2. `[SEC-CRITICAL]` → 即ブロック、PMへ
3. 要件不明確 → PMへ → 顧客確認

下流で無理に解決しない。問題は上流に伝播させる。


### オーナー確認が必要かの判断フロー

実装前に以下のチェックリストで判断する：

**チーム内で判断OK:**
- [ ] 既存パターンの踏襲である
- [ ] CLAUDE.md に判断基準が明記されている
- [ ] 技術的選択のみで、ビジネス判断を含まない
- [ ] 影響範囲が限定的（単一機能内で完結）

**オーナー確認が必要:**
- [ ] 新しいアーキテクチャパターンの導入
- [ ] ユーザー体験に直接影響する変更
- [ ] 複数の実装方針があり、トレードオフがある
- [ ] セキュリティ/データ保護に関わる判断
- [ ] 外部サービス/APIの選定

**迷ったら:** オーナー確認する（判断ミスより確認コストの方が低い）


## Worktree運用

並列作業のため、各エージェントは専用のworktreeで作業する。


### ディレクトリ構成

```
/tmp/maxam/{agent-name}-{instance}/{parent}_{child}/
```

- `{agent-name}`: エージェント名（例: yuki, rin, shiori）
- `{instance}`: インスタンス番号（例: 1, 2, 3）- 複数インスタンス運用時に使用
- 親ディレクトリからの相対パスを `_` で連結してフラットに管理する

| パス | 用途 |
|------|------|
| `~/{project}/` | 元リポジトリ（main）、PMの作業場所 |
| `/tmp/maxam/{agent}-{instance}/{project}/` | 各エージェント用worktree（単独リポジトリ） |
| `/tmp/maxam/{agent}-{instance}/{parent}_{child}/` | 各エージェント用worktree（ネストしたリポジトリ） |

**例：単独プロジェクト（単一インスタンス）**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/rin-1/MAXAM/         # Rin-1号機のMAXAM作業
```

**例：複数インスタンス運用**
```
/tmp/maxam/yuki-1/MAXAM/        # Yuki-1号機のMAXAM作業
/tmp/maxam/yuki-2/MAXAM/        # Yuki-2号機のMAXAM作業（並行実行）
/tmp/maxam/yuki-1/HOGEHOGE/     # Yuki-1号機のHOGEHOGE作業


---

*詳細はCLAUDE.md（フル版）を参照。チーム固有情報は.maxam/CLAUDE.mdを参照*
